import React from 'react';
import FormationsPage from '@/Pages/FormationsPage'; // Adjust the path if necessary

const Features: React.FC = () => {
    return (
        <section id="features" className="container mx-auto py-10 sm:py-20">
            <FormationsPage/>
        </section>
    );
}

export default Features;

